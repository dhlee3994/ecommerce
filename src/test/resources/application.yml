logging:
  level:
    org.hibernate:
      sql: debug
      orm.jdbc.bind: trace

spring:
  application.name: ecommerce
  profiles:
    active: test
  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true

cors:
  allowed-origins: http://localhost:3000,http://localhost:4000
